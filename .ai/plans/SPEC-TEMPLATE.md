# SPEC: {Feature Title}

> Copy this template to `.ai/plans/{YYYY-MM-DD}-{feature-slug}.md` (or a subdirectory for multi-document plans). Do not begin implementation until the user approves the contract.

## Mission Statement

_One or two sentences describing the goal and the problem it solves._

## Architectural Fit

- **Packages involved:** _(e.g., `packages/core`, `packages/vite-plugin`, `examples/basic-theme`)_
- **Components / modules touched:** _(e.g., new `ComponentInterface` implementation, CLI command, Vite plugin hook)_
- **Hooks & filters used:** _(e.g., `stratawp_theme_components`, `stratawp_conditional_css_files`)_
- **Skills referenced:** _(link the `.ai/skills/` and `.claude/skills/` recipes this plan relies on)_

## User Stories

- As a _(theme developer / framework user / site visitor)_, I want _..._ so that _..._

## Success Metrics

_How will this be verified? (e.g., "unit tests cover the new resolver", "pnpm ai:check passes", "no new axe-core violations in pnpm test:e2e")_

## Technical Plan (The Contract)

1. **Scaffolding:** _(commands to run, e.g., `stratawp component:new`, `stratawp block:new`)_
2. **Implementation steps:** _(logical order of file creation/modification, with paths)_
3. **Verification:** _(tests to add/run, manual checks)_

## Open Questions

- _(Anything unresolved that blocks >95% implementation confidence.)_

## Approval

- **Status:** Draft / Approved
- **Approved by:** —
- **Date:** —
